VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional. Set this to turn on the /maintenance page, which deletes uploaded test
# documents. Leave it unset and that page does nothing. It is a convenience gate and
# not authentication: the value is compiled into the bundle the browser downloads.
VITE_MAINTENANCE_KEY=
